# 新 Codex 任务实施提示词

以下“主提示词”可直接复制到新的 Codex 任务。确保新任务的工作目录包含同目录下的 `DEVELOPMENT_SPEC.md`。

该提示词按官方 OpenAI 提示建议组织：明确目标、上下文、硬性约束、授权边界、验收标准和交付格式，避免重复指令。参考：<https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices>

## 推荐主提示词

```text
请在当前工作区完整开发“Proxy Panel 单容器全功能版”。

首先完整阅读 DEVELOPMENT_SPEC.md，并将其作为产品需求、架构、安全、测试和验收的唯一主规格。然后检查当前仓库、AGENTS.md、已有代码和工作区状态。如果仓库为空，请按规格初始化项目；如果已有代码，请保留用户已有改动并在现有基础上继续。

目标：交付一个面向 Debian/Ubuntu Linux VPS 的单 Docker 容器应用。容器内同时运行网页面板、Snell 多实例、SS-2022 多实例和 ShadowTLS 多实例，使用 /data 持久化。完整功能模式采用 host 网络以及 NET_ADMIN、NET_RAW，但禁止 --privileged，禁止挂载 Docker Socket，禁止提供任意 Shell 执行功能。

采用以下技术方向：
- 后端和 Runtime Agent：Go。
- 前端：Vue 3 + TypeScript + Vite。
- 数据库：SQLite，版本化迁移。
- Web 与高权限 Agent：Unix Domain Socket 上的有类型白名单 RPC。
- 容器进程监督：s6-overlay 或等效轻量 init。
- 生产镜像中不保留 Node.js。
- 支持 linux/amd64 和 linux/arm64。

必须实现的认证行为：
- 登录不需要用户名，只有密码。
- 默认密码固定为 password。
- 首次登录后强制修改密码；改密前除认证状态、改密和退出外，所有管理 API 都返回 PASSWORD_CHANGE_REQUIRED。
- 新密码只要求 4–128 个 Unicode 字符、非空且不能继续等于 password，不增加其他复杂度规则。
- 使用 Argon2id 保存密码哈希。
- 改密或重置密码后注销全部 Session。
- 提供 panelctl reset-password password。

必须实现的默认部署：
- 面板默认监听 0.0.0.0:8080，Docker 部署后可直接通过服务器 IP 访问；文档必须提示只向可信 IP 放行管理端口并立即改密。
- Snell v5 默认监听 0.0.0.0:6160，随机 PSK。
- ShadowTLS v3 默认监听 0.0.0.0:8443/tcp，转发到 Snell，默认 SNI 为 www.microsoft.com，随机密码。
- 自动创建一个 SS-2022 TCP+UDP 节点，使用 2022-blake3-aes-128-gcm 和合规随机 Base64 密钥。
- 数据库、配置、密钥、日志、运行时和备份全部保存在 /data。

实现时遵守这些边界：
- 正常的本地代码修改、依赖安装、构建和非破坏性测试已获授权，可自主完成，不必逐步询问。
- 不部署到外部服务器，不发布镜像或仓库，不修改真实云安全组。
- 不执行 curl | bash、bash <(...) 或任何最新远程脚本。
- 阅读上游脚本只用于兼容分析；所有下载必须使用白名单 HTTPS 来源、超时、大小限制和哈希记录。
- 不在当前开发机自动修改 nftables/iptables。特权网络测试必须由 RUN_PRIVILEGED_TESTS=1 显式开启，并只在一次性 Linux VM/CI 中运行。
- 不进行破坏性 Git 操作，不覆盖无关用户改动。
- 遇到许可证、Snell 二进制分发权、未知上游破坏性变化或其他会实质改变产品范围的问题时，停止对应部分并明确报告；其余可安全开发的部分继续推进。

工作方式：
1. 阅读规格和仓库后给出简短实施计划，并持续更新状态。
2. 按 DEVELOPMENT_SPEC.md 的里程碑顺序实现，但不要只停留在脚手架或方案层。
3. 优先完成可运行的纵向闭环：认证 → SQLite → Web/Agent RPC → 假进程监督 → 真实协议适配 → 前端 → 安装与测试。
4. 每个阶段运行与风险相称的测试，发现问题直接修复。
5. 配置修改和更新必须使用“校验、快照、应用、健康检查、失败回滚”事务。
6. 所有用户输入通过结构化 Schema 校验，执行外部程序时使用参数数组，禁止 Shell 字符串拼接。
7. 完成后进行代码审查、安全检查、容器构建和文档核对。

核心验收标准：
- 单容器能构建并启动。
- 不使用 --privileged 或 Docker Socket。
- 默认密码 password 可登录且被强制改密。
- 改密前管理 API 被阻断；改密后旧 Session 全失效。
- Snell v5 + ShadowTLS 和 SS-2022 默认配置能生成并由 Agent 管理。
- 支持 Snell、SS-2022、ShadowTLS 节点 CRUD、启停、重启、日志和健康状态。
- 支持 /data 持久化、配置修订、备份和失败回滚。
- 提供 scripts/install.sh、scripts/update.sh、scripts/uninstall.sh。
- 提供清晰 README、配置说明、安全警告和故障排查。
- 单元测试、后端集成测试、前端类型检查/测试、生产构建和非特权容器测试全部通过。

如果完整功能无法在一个任务周期内全部达到，请先保证仓库始终可构建、可测试，并按规格中的里程碑持续实现；不要用静态假页面或未接线按钮冒充完成。任何尚未完成的功能必须在最终报告中逐项列出，不得笼统声称完成。

最终交付时请报告：
- 已实现功能。
- 关键架构和安全决策。
- 主要文件路径。
- 执行过的测试及结果。
- 尚未完成或受阻的内容。
- 本地构建、运行和首次登录步骤。
```

## 可选：第一阶段专用提示词

如果希望把开发拆成多个新任务，可先使用下面的提示词完成 M0–M2：

```text
请阅读 DEVELOPMENT_SPEC.md，并实现 M0、M1、M2：仓库初始化、数据库迁移、认证状态机、Vue 面板骨架、Web/Agent Unix Socket RPC，以及使用假协议程序验证的多进程监督和配置事务。

这一阶段不要下载或执行真实 Snell、SS-2022、ShadowTLS，也不要修改本机防火墙。必须完成默认密码 password、首次登录强制改密、改密前 API 阻断、Argon2id、Session 注销、CSRF、登录限速、/data 持久化、节点基础数据模型、假节点启停/崩溃恢复/日志、单元测试和集成测试。

请直接实施并验证，不要只给方案。完成后报告测试结果以及进入 M3 前仍需处理的接口或风险。
```

## 可选：真实协议接入提示词

```text
请阅读 DEVELOPMENT_SPEC.md，检查当前实现和测试，在现有代码上完成 M3：真实 Snell、shadowsocks-rust 和 ShadowTLS 运行时下载与版本管理、配置生成、多实例进程管理、依赖启动顺序、健康检查、客户端配置与二维码，以及默认 Snell v5 + ShadowTLS + SS-2022 初始化。

禁止执行上游远程脚本，禁止把用户输入拼入 Shell。下载必须限制到规格允许的官方来源并记录版本、URL 和 SHA256。Snell 二进制分发许可未确认前不要把二进制提交到仓库或直接内置进最终镜像。

请使用测试夹具和假二进制覆盖失败路径；只有明确处于一次性 Linux 测试环境时才运行真实协议冒烟测试。完成后构建单容器镜像并报告验证结果。
```

## 可选：网络、更新与发布提示词

```text
请阅读 DEVELOPMENT_SPEC.md，检查现有实现，并完成 M4–M6：专属 nftables/iptables 规则、流量统计与限额、月度重置、SS-2022 中国大陆来源 IP 屏蔽、上游兼容适配器、运行时更新和自动回滚、备份恢复、安装/更新/卸载脚本、多架构镜像和发布文档。

不得在普通开发机修改真实防火墙。所有特权测试必须由 RUN_PRIVILEGED_TESTS=1 显式开启并在一次性 Linux VM/CI 中执行。规则必须限定到本项目专属表/链/集合并可由 panelctl cleanup-firewall 幂等清理。未知上游更新不得直接执行或强制应用。

请完成实现、测试、安全审查和发布前验收，并明确列出任何因许可证或环境限制未能真实验证的部分。
```

## 使用建议

- 推荐先把“推荐主提示词”粘贴到新任务，让 Codex读取本目录的开发规格并持续开发。
- 如果希望人工逐阶段验收，可依次使用三个阶段提示词。
- 新任务应在 `/Users/wangyipeng/Desktop/node` 工作区打开，以便直接读取本文档和开发规格。
- 不要把整份开发规格重复粘贴进提示词；让新任务读取文件可以减少重复上下文，同时保留完整约束。
